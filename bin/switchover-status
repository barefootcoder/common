#! /bin/bash

echo -e "\n\n\n\n"

timerfile=timer-new
ls -lh `find ~/timer/ -name $timerfile -o -name ".$timerfile.sw?" ; find ~/.purple/ -name '[abs]*.xml' | grep -v ' '`

echo "===="
ps -p `/usr/bin/pgrep -f bin/vimS`
vagrant-exe switchover-status-part2
