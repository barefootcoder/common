#! /bin/bash

if [[ ! -d tmp ]]
then
	echo "run this from inside your git working copy, at the root" >&2
	exit 1
fi

/bin/cp -rpfv ~/local/sandbox/* tmp/
for script in t
do
	/bin/cp -pfv ~/bin/$script tmp/bin
done
